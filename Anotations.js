// Iremos iniciar a pasta do projeto
// Logo de cara usaremos o comando npm init para inicializar
// Em seguida os seguintes comandos: npm i --save-dev @babel/preset-env @babel/core @babel/cli babel-loader webpack webpack-cli regenerator-runtime core-js@3
// Podemos conferir se tudo foi instalado corretamente dentro do arquivo package.json

// Nome do arquivo de configuração do webpack, que ficará na raiz do projeto
// webpack.config.js

// Nosso código fonte, sempre fica dentro da pasta 'src'

// Também termos uma outra pasta pública, que conterá os arquivos gerados pelo webpack, como por exemplo
// bundle

// Dentro da pasta public irá conter, tudo que o usuario podera ver

// Podemos definir um timeout 'tempo'enquando rodamos o nosso script, após o tempo estipulado o script que 
// foi executado irá morrer

// Criamos uma pasta modules dentro da pasta src para que possamos separar os arquivos JS  e por nessa pasta